---
const typicalSignals = [
  "Trust me bro\" signals with no reasoning",
  "Pump their own bags with \"exclusive\" calls",
  "$99/month recurring with hidden fees",
  "Fake Lambos and rented mansions",
  "Delete losing trades, highlight winners",
  "FOMO tactics: \"Last chance!\" \"VIP only!\""
];

const tradeGPTApproach = [
  "Every signal backed by technical reasoning",
  "Objective analysis of liquid, major pairs only",
  "$65 one-time payment, no subscriptions",
  "Built by traders, for traders with day jobs",
  "Transparent track record, no cherry-picking",
  "Education-focused: Learn while you trade"
];
---

<section class="py-16 px-4 relative">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-black via-gray-900/30 to-black"></div>
  
  <div class="relative z-10 max-w-5xl mx-auto">
    <!-- Section Header -->
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        Not Another
        <span class="bg-gradient-to-r from-orange-400 to-orange-600 bg-clip-text text-transparent">Signal Scam</span>
      </h2>
      <p class="text-lg text-gray-400 max-w-2xl mx-auto">
        Here's how TradeGPT differs from the typical "crypto guru" nonsense
      </p>
    </div>
    
    <!-- Comparison Grid -->
    <div class="grid md:grid-cols-2 gap-6 max-w-4xl mx-auto">
      <!-- Typical Signal Services -->
      <div class="p-6 rounded-xl bg-red-500/5 backdrop-blur-sm border border-red-500/20">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-8 h-8 bg-red-500/20 rounded-lg flex items-center justify-center">
            <svg class="w-4 h-4 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-red-300">Typical Signal Services</h3>
        </div>
        
        <div class="space-y-3 text-sm">
          {typicalSignals.map(item => (
            <div class="flex items-start gap-2">
              <span class="w-1 h-1 bg-red-400 rounded-full mt-2 flex-shrink-0"></span>
              <span class="text-gray-400">{item}</span>
            </div>
          ))}
        </div>
      </div>
      
      <!-- TradeGPT Approach -->
      <div class="p-6 rounded-xl bg-green-500/5 backdrop-blur-sm border border-green-500/20">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-8 h-8 bg-green-500/20 rounded-lg flex items-center justify-center">
            <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-green-300">TradeGPT Approach</h3>
        </div>
        
        <div class="space-y-3 text-sm">
          {tradeGPTApproach.map(item => (
            <div class="flex items-start gap-2">
              <span class="w-1 h-1 bg-green-400 rounded-full mt-2 flex-shrink-0"></span>
              <span class="text-gray-300">{item}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
    
    <!-- Bottom CTA -->
    <div class="text-center mt-10">
      <p class="text-gray-400 mb-6">
        No hype. No BS. Just clear signals when the setup is right.
      </p>
      <a href="#pricing" class="inline-flex items-center gap-2 px-6 py-3 bg-orange-500 hover:bg-orange-600 text-white font-medium rounded-lg transition-all duration-200 hover:shadow-lg hover:shadow-orange-500/25">
        Try It Now
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
        </svg>
      </a>
    </div>
  </div>
</section>