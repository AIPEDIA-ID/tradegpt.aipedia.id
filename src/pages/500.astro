---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Server Error - TradeGPT Trading Assistant">
  <section class="py-20 px-4 relative min-h-[80vh] flex items-center justify-center">
    <!-- Background -->
    <div class="absolute inset-0 bg-gradient-to-b from-black via-gray-900/20 to-black"></div>
    
    <div class="relative z-10 max-w-3xl mx-auto text-center flex flex-col items-center justify-center">
      <!-- Glassy Container -->
      <div class="glassy-container relative max-w-md mx-auto mb-8 rounded-2xl p-8 bg-white/5 backdrop-blur-sm overflow-hidden">
        <!-- Animated Border Effect -->
        <div class="glassy-border"></div>
        
        <h1 class="text-4xl md:text-6xl font-bold mb-4 text-white">
          <span class="bg-gradient-to-r from-orange-400 to-orange-600 bg-clip-text text-transparent">500</span>
        </h1>
        
        <h2 class="text-2xl md:text-3xl font-semibold mb-6 text-white">Server Error</h2>
        
        <p class="text-lg text-gray-300 mb-8">
          Maaf, terjadi kesalahan pada server kami. Tim kami sedang bekerja untuk memperbaikinya.
        </p>
        
        <!-- Back Button -->
        <a href="/" class="px-8 py-4 bg-orange-500 hover:bg-orange-600 text-white font-semibold rounded-xl transition-all duration-200 hover:shadow-lg hover:shadow-orange-500/25 hover:scale-105">
          Kembali ke Beranda
        </a>
      </div>
    </div>
  </section>

  <style>
    .glassy-container {
      position: relative;
      z-index: 1;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    }
    
    .glassy-border {
      position: absolute;
      inset: 0;
      border-radius: inherit;
      padding: 1px;
      background: linear-gradient(
        var(--angle, 0deg),
        rgba(255, 255, 255, 0.05),
        rgba(255, 126, 0, 0.3),
        rgba(255, 255, 255, 0.1),
        rgba(255, 126, 0, 0.2)
      );
      -webkit-mask: 
        linear-gradient(#fff 0 0) content-box, 
        linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      mask-composite: exclude;
      pointer-events: none;
      animation: rotate 4s linear infinite;
    }
    
    @keyframes rotate {
      to {
        --angle: 360deg;
      }
    }
    
    @property --angle {
      syntax: '<angle>';
      initial-value: 0deg;
      inherits: false;
    }
  </style>
</Layout>